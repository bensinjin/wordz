import { StyleSheet } from 'react-native';

const colors = {
    white: 'white',
};

export const appStyles = StyleSheet.create({
    button: {
        backgroundColor: 'blue',
    },
    buttonText: {
        fontWeight: 'bold',
        color: colors.white,
    },
});